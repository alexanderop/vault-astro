---
// Mermaid blocks are rendered client-side by the mermaid library.
// This component is not used directly — instead, the rehype plugin
// transforms ```mermaid code blocks into <div class="mermaid"> elements,
// and the mermaid init script (in the layout) renders them.
---
